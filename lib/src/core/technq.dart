import 'package:flutter/material.dart';

class TechnqApp extends StatelessWidget {
  const TechnqApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: Scaffold(body: Center(child: Text('Halo'))));
  }
}
